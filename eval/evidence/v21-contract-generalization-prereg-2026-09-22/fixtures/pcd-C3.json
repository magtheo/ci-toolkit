{
  "expected_label": "REFUSES",
  "finding": {
    "comment": "the response is captured, status-checked, and its JSON returned, so delivery semantics are genuinely preserved",
    "file": "svc/dispatch.py"
  },
  "fixture": {
    "input": {
      "files": [
        {
          "patch": "--- a/svc/dispatch.py\n+++ b/svc/dispatch.py\n@@ -14,8 +14,9 @@\n # Refactor keeps identical delivery semantics to the old poster.\n import requests\n def dispatch(event):\n-    old_post(event)\n+    r = requests.post(WEBHOOK_URL, json=event, timeout=30)\n+    r.raise_for_status()\n+    return r.json()",
          "path": "svc/dispatch.py"
        }
      ]
    }
  },
  "id": "pcd-C3",
  "language": "python",
  "probe": false,
  "rationale": "same client, result captured/checked/returned",
  "relation": "preserved_claim_vs_dropped_call_result",
  "role": "control"
}
