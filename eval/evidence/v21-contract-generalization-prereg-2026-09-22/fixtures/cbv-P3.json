{
  "expected_label": "ADMITS",
  "finding": {
    "comment": "this.selection is cleared before the panel guard; when the panel is not mounted the selection is already consumed and the show request is silently dropped instead of deferred.",
    "file": "src/panels/viewer.ts"
  },
  "fixture": {
    "input": {
      "files": [
        {
          "patch": "--- a/src/panels/viewer.ts\n+++ b/src/panels/viewer.ts\n@@ -44,6 +44,10 @@\n   showLatest() {\n+    this.selection = null;\n+    const panel = this.panelRef;\n+    if (panel == null) return;\n+    this.render(panel);\n   }",
          "path": "src/panels/viewer.ts"
        }
      ]
    }
  },
  "id": "cbv-P3",
  "language": "typescript",
  "probe": true,
  "rationale": "property assignment consumption before guard",
  "relation": "consume_before_validate_ordering",
  "role": "positive"
}
