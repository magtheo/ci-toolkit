{
  "id": "C6",
  "kind": "control",
  "origin": "synthetic",
  "paired_with": "M6",
  "input": {
    "title": "review: skip patchless PRs early",
    "body": "Skips PRs with no textual changes before fetching the rubric.",
    "files": [
      {
        "path": "review.sh",
        "status": "modified",
        "patch": "--- a/review.sh\n+++ b/review.sh\n@@ -1,5 +1,12 @@\n # changed files were collected as JSONL (one object per line):\n #   jq -c '.[]' <<<\"$batch\" >>\"$files_jsonl\"\n+if ! jq -se 'any(.[]; .patch != null)' \"$files_jsonl\" >/dev/null; then\n+  echo \"no textual changes to review (docs/binary-only?)\"\n+  exit 0\n+fi"
      }
    ]
  },
  "expected": {
    "assessment": "CLEAR",
    "findings": []
  },
  "note": "Globally clean: -se slurps the JSONL into one array."
}
