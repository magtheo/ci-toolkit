{
  "id": "M9",
  "kind": "positive",
  "origin": "synthetic",
  "paired_with": "C9",
  "family": "hallucinated-fact",
  "input": {
    "title": "ci: gate deploy on PR merge state",
    "body": "Adds a merge-state guard before deploy.",
    "files": [
      {
        "path": "scripts/deploy-guard.sh",
        "status": "added",
        "patch": "--- /dev/null\n+++ b/scripts/deploy-guard.sh\n@@ -0,0 +1,10 @@\n+#!/usr/bin/env bash\n+# Deploys only after the PR is merged. jq -e exits 0 when the\n+# expression is true, 1 when false \u2014 the guard branches on that.\n+set -euo pipefail\n+pr=$(cat)\n+if jq -e '.mergedAt == null' <<<\"$pr\" >/dev/null; then\n+  echo \"deploy: PR merged \u2014 continuing\"\n+else\n+  echo \"deploy: PR still open \u2014 aborting\" >&2\n+  exit 1\n+fi\n"
      }
    ]
  },
  "expected": {
    "assessment": "ISSUES_FOUND",
    "findings": [
      {
        "severity": "blocking",
        "comment_all": [
          "merged"
        ],
        "comment_any": [
          "inverted",
          "swapped",
          "reversed",
          "wrong direction",
          "== null"
        ]
      }
    ]
  },
  "note": "Isolated defect: condition inverted \u2014 merged PRs take the abort branch."
}
