{
  "id": "C1",
  "kind": "control",
  "origin": "synthetic",
  "paired_with": "M1",
  "input": {
    "title": "ci: wire up the ai-review caller",
    "body": "Adds the reusable reviewer workflow.",
    "files": [
      {
        "path": ".github/workflows/ai-review.yml",
        "status": "added",
        "patch": "--- /dev/null\n+++ b/.github/workflows/ai-review.yml\n@@ -0,0 +1,9 @@\n+name: ai-review\n+on:\n+  pull_request_target:\n+    branches: [main]\n+jobs:\n+  review:\n+    uses: some-org/ci-toolkit/.github/workflows/ai-review.yml@0123456789abcdef0123456789abcdef01234567\n+    with:\n+    secrets:\n+      LLM_API_KEY: ${{ secrets.LLM_API_KEY }}\n"
      }
    ]
  },
  "expected": {
    "assessment": "CLEAR",
    "findings": []
  },
  "note": "Globally clean: identical except the explicit single-secret mapping."
}
